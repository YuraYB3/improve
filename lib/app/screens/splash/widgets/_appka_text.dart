part of '../splash_screen.dart';

class _AppkaText extends StatelessWidget {
  const _AppkaText({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      'APPKA',
      style: TextStyle(
        color: Colors.white,
        fontSize: 92,
      ),
    );
  }
}
